module.exports = {
  tableNames: {
    'product.csv': 'products',
    'features.csv': 'product_features',
    'styles.csv': 'styles',
    'skus.csv': 'skus',
    'photos.csv': 'photos',
    'related.csv': 'related_products',
    'cart.csv': 'cart',
    'test1.csv': 'test1',
    'test2.csv': 'test2',
    'test3.csv': 'test3',
  },
  dataTypes: {
    products: {
      id: 'INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY',
      name: 'VARCHAR(100)',
      slogan: 'VARCHAR(255)',
      description: 'TEXT',
      category: 'VARCHAR(50)',
      default_price: ['INTEGER NOT NULL'],
    },
    product_features: {
      id: 'INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY',
      product_id: 'INTEGER',
      feature: 'VARCHAR(100)',
      value: 'VARCHAR(100)',
      foreign_key: ['FOREIGN KEY (product_id) REFERENCES products(id)'],
    },
    styles: {
      id: 'INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY',
      product_id: 'INTEGER',
      name: 'VARCHAR(100)',
      sale_price: 'VARCHAR(20)',
      original_price: 'VARCHAR(20)',
      default_style: 'BOOLEAN',
      foreign_key: ['FOREIGN KEY (product_id) REFERENCES products(id)'],
    },
    skus: {
      id: 'INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY',
      style_id: 'INTEGER',
      size: 'VARCHAR(20)',
      quantity: 'INTEGER',
      foreign_key: ['FOREIGN KEY (style_id) REFERENCES styles(id)'],
    },
    photos: {
      id: 'INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY',
      style_id: 'INTEGER',
      url: 'TEXT',
      thumbnail_url: 'TEXT',
      foreign_key: ['FOREIGN KEY (style_id) REFERENCES styles(id)'],
    },
    related_products: {
      id: 'INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY',
      product_id: 'INTEGER',
      related_product_id: 'INTEGER',
      foreign_key: [
        'FOREIGN KEY (product_id) REFERENCES products(id)',
        'FOREIGN KEY (related_product_id) REFERENCES products(id)',
      ],
    },
    cart: {
      id: 'SMALLINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY',
      user_session: 'SMALLINT',
      product_id: 'INTEGER',
      active: 'BOOLEAN',
      foreign_key: ['FOREIGN KEY (product_id) REFERENCES products(id)'],
    },
  },
};
