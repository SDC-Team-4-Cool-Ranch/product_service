// Organize configuartion files -> export environment variables to other parts + database configurations (ESSENTIAL)

module.exports = {
  development: {
    // example --------------
    // username: DB_USERNAME,
    // password: DB_PASSWORD,
    // database: DB_DATABASE,
    // host: DB_HOST,
    // dialect: 'postgres'
  },
};
